[project]
name = "bp_customer_portal"
authors = [
    { name = "BP Great North", email = "support@bpgreatnorth.com" },
]
description = "Customer portal API for the BP Great North order management mobile app"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "bp_customer_portal"
